define(function (require) {

  require(["lodash"],
  function (lodash) {
      
  });
  // // Load any app-specific modules
  // // with a relative require call,
  // // like:
  // var myModule = require('util')
  // myModule.getLuck()
  

});

